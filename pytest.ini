[pytest]
addopts = -m "not slow"
norecursedirs = tmp
markers =
    slow: real-model / network-dependent integration tests, excluded by the default addopts (run explicitly with `-m slow`, which overrides it)
