# Optional. Base URL the frontend uses to reach the backend API.
# If unset, the app falls back to http://127.0.0.1:8000 (the default
# local backend address), so local development works without a .env
# file.
VITE_API_BASE_URL=http://127.0.0.1:8000
